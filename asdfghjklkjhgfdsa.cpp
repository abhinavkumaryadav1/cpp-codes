#include<iostream>
using namespace std;
int main()
{
     #ifndef ONLINE_JUDGE
   freopen("input.txt", "r", stdin);
   freopen("output.txt", "w", stdout);
   #endif

    char names[1][10]={n}; 
    
    cin>>n;

int i;
char temp[10];
cout<<"the given array is:-\n";
 {
    printf(n);
 }
{
    for(i=o,i<10-1,i++)
}

{
    if(strcmp(names[1],names[i])>0)
    {
        
        strcpy(temp,names[i]);
        strcpy(names[1],names[i]);
        strcpy(names[i],temp);
    }




}
cout<<"the sorted array is\n";
for(i=1,i<10-1,i++)
{
    printf("%s",names[i]);

}
return 0;
}